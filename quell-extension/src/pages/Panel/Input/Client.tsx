import React, { useState, useEffect } from 'react';
import Button from '@mui/material/Button';

const Client = (props) => {

  const tableData:string[] = [
    [title1, col_1, col_2, col_3]],
    [row1, data1a, data2a, data3a],
    [row2, data1b, data2b, data3b],
    [row3, data1c, data2c, data3c]
  ];

  const genTable = (arr) => {
    const output = [];
    arr.forEach(el => )
  }
            

  return (
    <div>
      <Button>Submit Query</Button>
    </div>
  );
};

export default Client;